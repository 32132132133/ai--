```python
# 두 정수 A와 B를 입력받는다.
A, B = map(int, input().split())

# A+B를 출력한다.
print(A + B)
```

**예시 입력 및 출력**

```
입력: 1 2
출력: 3
```

**설명**

1. `map(int, input().split())` 함수를 사용하여 두 정수 A와 B를 입력받는다.
2. `print(A + B)` 함수를 사용하여 A+B를 출력한다.

**참고**

* `map(int, input().split())` 함수는 첫째 줄에 입력된 값을 공백을 기준으로 나누고, 각각을 정수형으로 변환하여 리스트로 반환한다.
* `print(A + B)` 함수는 A와 B의 값을 더하여 출력한다.
